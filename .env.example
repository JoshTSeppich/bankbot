# The discovery loop is the only place the system talks to a model. Replay
# never reads this file. Copy to .env (gitignored); python-dotenv loads it.

# Anthropic key for the Claude Messages API (vision + tool use).
ANTHROPIC_API_KEY=

# Only needed when ANTHROPIC_API_KEY is org-scoped rather than scoped to one
# workspace. A workspace-scoped key does not need this. Leave blank otherwise.
ANTHROPIC_WORKSPACE_ID=

# Credentials the demo bank app accepts. Replay resolves SecretRefs from these
# by name at run time; the values never enter an artifact or a log.
BANKBOT_USERNAME=teller
BANKBOT_PASSWORD=teller-demo-password

# Fault flags the demo app reads once at startup; the same fields can be set
# at run time through POST /admin/faults. Blank means off.
BANKBOT_SESSION_EXPIRY_AT_STEP=
BANKBOT_UNKNOWN_DIALOG_AT_STEP=
BANKBOT_SLOW_LOAD_MS=

# Two more faults, and the only ones that are not markup: the browser's own
# alert() and confirm(). No ARIA snapshot and no screenshot shows either, so
# these are what test whether the system notices a question it was never
# shown. A confirm is dismissed and escalated; nothing here ever accepts one.
BANKBOT_NATIVE_ALERT_AT_STEP=
BANKBOT_NATIVE_CONFIRM_AT_STEP=

# Show the browser window instead of running headless. The handoff needs it:
# a person takes over the same window the engine opened. Anything but blank,
# 0 or false counts as on. `make replay-dialog` sets it for you.
HEADED=
